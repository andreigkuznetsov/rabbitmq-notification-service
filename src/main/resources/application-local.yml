spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/notifications
    username: postgres
    password: postgres

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest